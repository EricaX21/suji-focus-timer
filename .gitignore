# 依赖：用 npm install 还原，不进版本库
node_modules/

# 打包产物：每次打包重新生成（600M+）
dist/

# 本地测试数据（真实数据在 %APPDATA%\study-timer，不在仓库）
data/

# 编辑器 / 工具本地配置
.obsidian/
.claude/settings.local.json

# 系统杂物
Thumbs.db
desktop.ini
*.log
